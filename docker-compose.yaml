services:
  python_service:
    build: .
    depends_on:
      - psql-db

  psql-db:
    image: postgres:14
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: 123456
    ports:
      - "5432:5432"
